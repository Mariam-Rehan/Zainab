# Local pip install. On Streamlit Cloud, environment.yml is used instead (Python 3.12).
# If you deploy with requirements.txt only, pick Python 3.12 in Advanced settings (not 3.14).
tensorflow>=2.15,<2.22
yfinance>=0.2.40
pandas>=2.0,<3.1
numpy>=1.24,<2.5
scikit-learn>=1.3,<1.9
streamlit>=1.30,<2
joblib>=1.3
